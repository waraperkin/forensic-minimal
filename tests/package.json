{
  "name": "forensic-minimal-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --project=ui",
    "test:services": "playwright test --project=playwright"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
